{
  "name": "transcendence-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node ./node_modules/typescript/bin/tsc -w -p tsconfig.json",
    "build": "prisma generate && node ./node_modules/typescript/bin/tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "prisma generate && node --test tests/integration/*.mjs",
    "prisma:generate": "prisma generate",
    "prisma:format": "prisma format",
    "prisma:validate": "prisma validate"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@prisma/client": "^6.16.0",
    "dotenv": "^17.2.3",
    "fastify": "^5.6.2",
    "otplib": "^12.0.1",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "prisma": "^6.16.0",
    "typescript": "^5.9.3"
  }
}
